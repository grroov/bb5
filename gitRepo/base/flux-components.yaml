apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-components
  namespace: bigbang
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: bigbang
  path: ./base/flux
  prune: true
